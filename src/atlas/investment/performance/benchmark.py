
"""Benchmark comparison placeholder."""

from __future__ import annotations


def benchmark_snapshot() -> dict:
    return {
        "benchmark": "equal_weight_BTC_ETH_SOL",
        "status": "placeholder",
        "note": "Benchmark return tracking will be enabled once historical portfolio snapshots are accumulated.",
    }
